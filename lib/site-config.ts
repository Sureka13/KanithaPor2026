// The one line that differs between the main (real competition) branch and
// the demo-testing branch — tags every submission so admin can tell them
// apart even though both currently point at the same Supabase project.
export const SITE_SOURCE: "production" | "demo" = "production";
